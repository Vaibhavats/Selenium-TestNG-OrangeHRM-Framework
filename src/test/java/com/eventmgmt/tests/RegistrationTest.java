package com.eventmgmt.tests;

import com.eventmgmt.base.BaseTest;
import org.testng.annotations.Test;

/**
 * RegistrationTest.java
 * ----------------------
 * TEMPORARILY DISABLED:
 * This test class is disabled because the current test environment
 * (OrangeHRM/demo site) does not support registration workflows
 * defined for the Event Management System.
 *
 * These tests can be re-enabled once a compatible UI is available.
 */

@Test(enabled = false)
public class RegistrationTest extends BaseTest {

    // Entire class disabled intentionally
    // No test methods will execute

}